services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./tic-tac-toe/dist:/usr/share/nginx/html 
      - ./tic-tac-toe/.nginx/nginx.conf:/etc/nginx/nginx.conf:ro 
    container_name: react-container
    restart: always